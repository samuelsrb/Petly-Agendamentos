{
  "name": "petly",
  "description": "Web application for scheduling appointments at the pet shop.",
  "author": "Samuel Soares",
  "scripts": {
    "server": "json-server --watch server.json --port 3333"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.21"
  }
}
